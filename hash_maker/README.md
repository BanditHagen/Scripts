Description:

Python program that generates MD5 hashes based on user input.

Use:

"./hash_creator.py" to run from terminal.
programe will ask for intut in numeric form:

lenght of "password"
number of "passwords" to generate.

for Linux users:
run: "./hash_creator.py > resault.txt" to save resaults in a separate text file.


 
